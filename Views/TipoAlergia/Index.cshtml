@model IEnumerable<WebNinnoFeliz.Models.TipoAlergia>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Tabla de tipo de alergias</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
</p>
<table class="table">
    <thead class="table-dark">
        <tr>
            <th>
                Id tipo alergia
            </th>
            <th>
                Nombre alergia
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoAlergia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoAlergia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoAlergia"class="btn btn-success">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoAlergia"class="btn btn-info">Detalle</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoAlergia"class="btn btn-danger">Borrar</a>
            </td>
        </tr>
        }
    </tbody>
</table>
