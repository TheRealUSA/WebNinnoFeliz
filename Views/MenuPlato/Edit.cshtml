@model WebNinnoFeliz.Models.MenuPlato

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>MenuPlato</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdnumeroMenuPlato" />
            <div class="form-group">
                <label asp-for="IdNumeroMenu" class="control-label"></label>
                <select asp-for="IdNumeroMenu" class="form-control" asp-items="ViewBag.IdNumeroMenu"></select>
                <span asp-validation-for="IdNumeroMenu" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPlato" class="control-label"></label>
                <select asp-for="IdPlato" class="form-control" asp-items="ViewBag.IdPlato"></select>
                <span asp-validation-for="IdPlato" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index"class="btn btn-dark">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
