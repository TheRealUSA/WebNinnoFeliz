@model IEnumerable<WebNinnoFeliz.Models.Menu>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Tabla de menú</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
    <a asp-action="PDF" class="btn btn-primary">PDF</a>
</p>
<table class="table">
    <thead class="table-dark">
        <tr>
            <th>
                Id numero menú
            </th>
            <th>
                Nombre menú
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdNumeroMenu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMenu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdNumeroMenu"class="btn btn-success">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdNumeroMenu"class="btn btn-info">Detalle</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNumeroMenu"class="btn btn-danger">Borrar</a>
            </td>
        </tr>
        }
    </tbody>
</table>
