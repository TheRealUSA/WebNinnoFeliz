@model WebNinnoFeliz.Models.AbonadorCargoMensuale

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>AbonadorCargoMensual</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdAbonadorCargoMensual" />
            <div class="form-group">
                <label asp-for="IdAbonador" class="control-label"></label>
                <select asp-for="IdAbonador" class="form-control" asp-items="ViewBag.IdAbonador"></select>
                <span asp-validation-for="IdAbonador" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdCargo" class="control-label"></label>
                <select asp-for="IdCargo" class="form-control" asp-items="ViewBag.IdCargo"></select>
                <span asp-validation-for="IdCargo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
            <div class="form-group">
                <a asp-action="Index" class="btn btn-dark">Back to List</a>
            </div>
            <div class="form-group">
                <input />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
